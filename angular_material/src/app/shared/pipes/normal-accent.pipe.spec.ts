import { NormalAccentPipe } from './normal-accent.pipe';

describe('NormalAccentPipe', () => {
  it('create an instance', () => {
    const pipe = new NormalAccentPipe();
    expect(pipe).toBeTruthy();
  });
});
