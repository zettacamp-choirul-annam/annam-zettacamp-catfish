import { CombineWordsPipe } from './combine-words.pipe';

describe('CombineWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new CombineWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
